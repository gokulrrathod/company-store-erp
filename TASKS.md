# VMG ERP Node/Postgres Demo — Task Tracker

This tracks progress on the **demo build** in `Store POC/` (Node + Express + PostgreSQL + React/MUI/ag-Grid).
This is a parallel, separate effort from the official `.NET`/Azure ERP analysis in
`.claude/ERP Software Planning/` — that folder is never modified from this project.

Architecture decisions locked in (see `.claude/ERP Software Planning/Demo-Roadmap-Node-Stack.md`):
- One shared Express backend for all departments (no per-module microservices).
- ag-Grid Community for every list/table.
- Large/multi-row forms are dedicated routed pages; small forms are dialogs.
- zod validation mirrored on frontend (react-hook-form) and backend (Express middleware).
- Project folder stays named `Store POC` (rename attempt abandoned — folder was locked).

## Done

All 8 departments built, wired into one Express/Postgres app, and covered by passing Playwright e2e tests.

- [x] **Store** — Inventory, PO→GRN→Inspection→Approval, Stock Movements, Material Requests, Low Stock, Rejected Material, Housekeeping, Safety, Reports
- [x] **Sales** — Enquiry → Quotation → Negotiation → Confirm → Production status → Payment → Dispatch → Delivered
- [x] **Purchase** — Vendor Master (Purchase→Finance→Management approval chain), Purchase Orders, Budgets
- [x] **Design** — Drawings, BOM, sequential approval (Engineer → Checker w/ 13-item checklist → Design Head → Customer → Released), ECN
- [x] **Production** — Weekly Plans → Head Approval → Activities/Schedules → Stage Inspection → Rework log
- [x] **Project / Civil** (merged per Recommended-Decisions) — Projects → DPR → Measurement Book → RA Bills → Reconciliation-gated Closure, live Excess/Saving computation
- [x] **Transport** — Vehicle Master (unique number), Insurance Records history (renewal = new row, never overwritten), 30/60/90-day expiry dashboard, Management read-only
- [x] **Accounts** — Unified Invoice entity (Purchase + Sales), three-way match gate (PO ↔ Approved GRN ↔ Invoice) before invoice booking, Payments with outstanding-balance cap
- [x] Sales dispatch's Vehicle field converted from free text to a lookup into the Transport Vehicle Master (Requirements-Transport.md AC4)
- [x] Full Docker rebuild + health check after every schema change
- [x] e2e regression: 13 tests across `store-sop`, `sales-workflow`, `sales-discount-approval`, `purchase-workflow` (x2), `design-workflow`, `production-workflow`, `project-workflow`, `transport-workflow`, `accounts-workflow` — all passing with `--workers=1`

- [x] **Combined full-demo e2e script** (`e2e/tests/full-demo.spec.js`) — walks all 8 modules end-to-end in one paced, narratable run. Verified: passed first run against a freshly reset Docker stack, ~5 minutes.
- [x] **Demo video recorded** — `Store POC/demo-videos/VMG-ERP-Full-Demo-2026-08-21.webm` (~9 MB, ~5 min, screen-captured via Playwright's built-in video recorder at 1440×900). Covers Store → Sales (incl. Transport vehicle-lookup dispatch) → Purchase → Design → Production → Project/Civil → Transport → Accounts.

### Test-suite fix + re-record (this session, 2026-08-21)

- [x] **Found and fixed a real test-isolation bug**: `full-demo.spec.js` ran alphabetically before `production-`, `project-`, and `purchase-workflow.spec.js` and seeded overlapping fixture data (same vendor/project names), so running the full suite together caused 3 spurious failures (strict-mode duplicate-element violations, not-found errors). Fixed by excluding `full-demo.spec.js` from the default `playwright test` run (`testIgnore` in `e2e/playwright.config.js`, gated on `INCLUDE_DEMO` env var) and adding `npm run test:demo` to run it in isolation. Full 13-test regression now passes cleanly on a freshly reset stack; `full-demo.spec.js` also verified independently.
- [x] **Re-recorded all demo videos** against the current UI (post sidebar/header/notifications/grid-density overhaul) — `demo-videos/2026-08-21-latest/`: one combined narrated walkthrough (`00-full-demo-all-8-modules`) plus 13 individual per-workflow videos (one per regression test, `01`–`13`), giving separate close-up footage of each module instead of only the single combined video. Old stale `VMG-ERP-Full-Demo-2026-08-21.webm` removed.
- [x] **Converted all demo videos from WebM to MP4** (`ffmpeg -c:v libx264 ... -movflags +faststart`) — Playwright's recorder only outputs WebM, which iPhone Safari cannot play at all (no decoder); MP4/H.264 plays everywhere including iOS. Also ~2.5x smaller. WebM originals removed from the repo.
- [x] **Role-access audit against SOP requirements**: cross-checked all 19 roles' sidebar visibility and backend `requireRole` gates against `.claude/ERP Software Planning/Requirements/*.md`. Found and fixed one real violation — Purchase (and every other role) could read BOM lines on Draft/Under-Checking drawings via `GET /drawings`, violating Requirements-Design.md §11 AC5. Now restricted to the Design team (Design Engineer, Checker, Design Head, Admin) only; everyone else sees Released drawings only.
- [x] **Wrote `VMG-ERP-Role-Access-and-Test-Report.docx`** — full role/menu matrix, all 8 module workflows, the complete 51-endpoint permission table, and live test results (56/56 role-gate checks passed against a running backend, both local and Railway).
- [x] **Found and fixed a process-crashing bug** discovered while live-testing role gates: `PATCH /projects/:id/reconciliation` had no try/catch, so a DB error (FK violation) became an unhandled promise rejection that killed the whole Node process — Express 4 doesn't auto-forward async rejections to error middleware. Root cause was systemic (97 async handlers across every route file had the same latent risk), so fixed with a shared `asyncHandler()` wrapper (`backend/src/middleware/asyncHandler.js`) applied to all of them, not just the one handler. Verified: same request now returns a normal 500 and the backend stays up, locally and on Railway. Full 13-test regression still green.

- [x] **Testing-steps document** — `Store POC/TESTING-GUIDE.md`. One section per department (Store, Sales, Purchase, Design, Production, Project/Civil, Transport, Accounts) with sub-sections per role/login, listing exact credentials and step-by-step manual actions, cross-referenced to the matching automated spec file at the bottom.

### UI/UX overhaul (this session)

- [x] **Sidebar rearranged into department sections** (`frontend/src/components/Sidebar.jsx`) — grouped under `NAV_GROUPS` (Overview, Sales, Store, Purchase, Design, Production, Project/Civil, Transport, Accounts, Reports) with `ListSubheader`s, instead of one flat list.
- [x] **Collapsible sidebar** — toggle button collapses to icon-only rail (`SIDEBAR_WIDTH_COLLAPSED = 72`) with tooltips; state persisted in `localStorage` (`useSidebarCollapsed` hook).
- [x] **Header search box** — MUI Autocomplete next to the user chip, searches `NAV_ITEMS` (role-filtered) and navigates on selection.
- [x] **Header notification bell** — now backed by a real backend endpoint (`GET /api/notifications`, `backend/src/routes/notifications.js`) that returns **role-scoped, actionable** items only (e.g. Quality sees GRNs awaiting inspection, Finance sees vendors/POs awaiting their verification, Transport sees vehicles needing insurance attention, Admin sees everything). Previously this was a generic client-side feed everyone saw the same version of — now each of the 18 roles gets a distinct, relevant list. Badge count reflects the filtered list.
- [x] **Fixed-viewport app shell** (`frontend/src/components/NavShell.jsx`) — header, sidebar, and footer are pinned within a `height: 100vh` shell; only the main content column scrolls internally when its own content overflows. Fixed a real bug where the footer sat outside the `100vh` budget, forcing a page-level scrollbar even on short pages.
- [x] Full e2e regression (13 tests) re-verified green after each of the above changes.

### Grid & form density pass (this session)

- [x] **ag-Grid density** (`frontend/src/components/DataTable.jsx`) — `domLayout="autoHeight"` so the grid grows to fit its rows instead of scrolling internally (the page/content column scrolls instead, per the fixed-viewport shell); row height 34px / header 36px (was ~42px default); default page size bumped 20→50.
- [x] **Removed per-column filter icons and column menu clutter** — `defaultColDef` now `filter: false, suppressMenu: true` (sort + resize kept). Quick-filter search boxes (e.g. Inventory) remain the primary search mechanism.
- [x] **Smaller controls globally** — `frontend/src/theme/vmgTheme.js` sets `size: 'small'` as the default for `MuiTextField`, `MuiButton`, `MuiSelect`, `MuiAutocomplete`, `MuiFormControl`, `MuiIconButton`, `MuiChip`, instead of each page opting in individually.
- [x] **Labeled the two placeholder-only inputs** — Inventory's quick-search box and the header's menu-search Autocomplete now have a proper `label` (not just a `placeholder`), matching every other form field in the app.
- [x] Full e2e regression (13 tests) re-verified green after these changes.

## SOP Compliance Gap Remediation (Analyst Review, 2026-08-22)

Cross-checked all 8 `Requirements-*.md` docs against the actual schema/routes/frontend (facts verified via direct grep/read of the codebase, not assumption). Full findings in session notes; task backlog below, ordered by severity. Working through it one item at a time (started 2026-08-22) — each item gets a real migration (via the new `backend/src/db/migrate.js` runner, since schema.sql only applies on first Postgres init and never re-applies to Railway's already-initialized DB), backend + frontend wiring, and a live end-to-end verification before being checked off.

### Tier 0 — explicit hard SOP rules currently violated

- [x] **Purchase: link PO to MR.** ~~`purchase_orders` has no FK to `material_requests` at all~~ — Fixed 2026-08-22. `po_lines.mr_id` (nullable FK) added; Store Manager can now `PATCH /material-requests/:id/forward` a PENDING request to `FORWARDED_TO_PURCHASE` when stock isn't available (closes the previously-unimplemented "Stock Not Available → Forward to Purchase" branch from Store §2); Purchase Order creation can link any line to a forwarded MR (auto-fills item/qty, marks the MR `PO_RAISED`); once the linked GRN is approved, Store re-actions the same MR through the existing approve/issue path. Verified live end-to-end (forward → PO → GRN → approve → issue), including double-forward and double-link rejection. Caught and fixed a real bug along the way: `material_requests.status` was `VARCHAR(20)`, too narrow for `FORWARDED_TO_PURCHASE` (21 chars) — widened to `VARCHAR(30)` via migration `002`.
- [x] **Store: enforce FIFO/FEFO at Material Issue.** ~~Material Issue decrements `items.quantity` as one pool with no batch/expiry ordering~~ — Fixed 2026-08-22. New `item_batches` table (migration `003`) is the real per-batch stock ledger; existing stock backfilled into `OPENING` batches so nothing broke. `backend/src/services/stock.js` centralizes FEFO-then-FIFO consumption (nearest expiry first, then oldest-received), wired into every stock-decrease/increase path (Material Request approval, manual Stock Movement IN/OUT, item creation, GRN approval). Overriding the suggested batch requires an explicit reason, enforced server-side; the Material Requests approve dialog surfaces available batches and only demands a reason when a non-suggested one is picked. Verified live: FEFO ordering (soonest-expiry batch listed first regardless of receipt order), a single issue correctly splitting across two batches, override rejected without a reason and succeeding with one (drawing from the chosen batch, not the suggested one), and over-issue still cleanly rejected. (Store §2, AC3)
- [x] **System-wide: audit trail.** ~~No `audit_log` table exists~~ — Fixed 2026-08-22. Implemented as a generic Postgres trigger (`audit_log_row_changes`, migration `004`) attached to every substantive table (items, item_batches, suppliers, purchase_orders, receipt_lines, material_receipts, material_requests, enquiries, sales_orders, drawings, engineering_change_notices, production_plans, production_schedules, projects, reconciliations, vehicles, invoices) — field-by-field diff via `to_jsonb(OLD)` vs `to_jsonb(NEW)`, enforced at the DB level so no route (present or future) can bypass it. The "who made this change" problem — triggers have no direct access to the app user — is solved with zero per-route changes: `AsyncLocalStorage` (new `middleware/requestContext.js`) carries the authenticated user's name for the life of each request, `requireAuth` seeds it once, and `db/pool.js` stamps it into a session GUC on every connection checkout before any query runs; every existing route already goes through the same `pool.query()`/`pool.connect()`. New Admin-only `GET /api/audit-log` (filterable by table/record) and an Audit Trail page. Verified live: non-admin correctly forbidden, field diffs accurate, correct user attribution including under concurrent requests from two different users (proving no cross-attribution). (Purchase §5)
- [x] **Store: wire up `damaged_stock`/`rejected_stock`.** ~~nothing ever writes to them~~ — Fixed 2026-08-22. GRN approval on a `PARTIALLY_ACCEPTED` line still receives the stock physically (`items.quantity`, unchanged behavior) but now also adds the full line quantity to `damaged_stock` (no split accepted/damaged quantity exists at inspection, so the whole line is flagged unusable until Store reconciles). Rejected Material creation increments `rejected_stock` as a separately tracked bucket (not double-subtracted from `quantity`, since `REJECTED` GRN lines were already excluded from it). `available_stock` formula fixed to also subtract `rejected_stock` (was previously only subtracting `reserved_stock` and `damaged_stock`). Both columns now visible on the Inventory grid, matching Store §1's Inventory Item entity fields. Verified live: a partially-accepted GRN correctly zeroes available stock for that batch while a separate clean GRN stays available, and a rejection entry correctly reduces available stock without double-counting against quantity.
- [x] **Accounts: itemize invoices.** ~~No `invoice_lines` table~~ — Fixed 2026-08-22, closing out Tier 0. New `invoice_lines` table (migration `005`); existing invoices backfilled with a single synthetic line each. `taxable_amount`/`total_amount` are now computed server-side from the sum of line amounts rather than trusted as a client-supplied lump sum. Purchase invoice creation pre-fills lines from the selected PO's items (current `unit_rate`) once three-way-matched; Sales invoices take manual lines (sales orders don't carry item-level data in this schema); picking an item on any line auto-fills description/rate. Invoice detail page shows the full line breakdown. Verified live: legacy backfill correct, server-computed totals match hand-calculated expected values exactly, HSN/SAC and per-line amounts persist correctly, empty-lines submission rejected.

**All five Tier 0 items are now done.** Moving to Tier 1 (whole missing entities) next.

### Tier 1 — whole entities missing

- [x] **Design: Design Input Sheet entity.** ~~pipeline step before Calculation, currently skipped entirely~~ — Fixed 2026-08-22. New `design_input_sheets` table (migration `007`), one per drawing (this schema's stand-in for "Design Project"): Customer Specification, Process Data, Applicable Standards, Material Specification, Corrosion Allowance, Design Pressure, an optional self-referencing Previous Reference Drawing, Design Notes, plus real file attachments via the upload capability above. `POST/PATCH /drawings/:id/input-sheet` for create/edit while DRAFT; `PATCH .../input-sheet/status` enforces AC1 server-side (cannot mark Completed without Customer Specification and Applicable Standards). Verified live: null before creation, duplicate creation rejected, completion blocked until required fields present then succeeds once they are, self-reference resolves correctly, editing a Completed sheet rejected, attachment upload round-trips.
- [x] **Design: Design Calculation entity.** ~~also currently skipped~~ — Fixed 2026-08-22. New `design_calculations` table (migration `008`), auto-numbered (`CALC-YYYY-NNNN`), multiple per drawing: Formula Reference, Safety Factor, Load/Shaft/Bearing/Motor/Gearbox calcs, Remarks, plus file attachments (Excel/PDF). Creation gated server-side on the linked Design Input Sheet being Completed (Requirements-Design.md §3's pipeline order), not just hidden in the UI. Verified live: blocked with no input sheet, blocked while input sheet is DRAFT, succeeds once Completed, numbers correctly increment, newest-first listing, attachment upload works.
- [x] **Design: real document-control/revision history.** ~~Only a single `revision` string on `drawings`; no per-revision record of prepared/checked/approved-by + date~~ — Fixed 2026-08-22. New `drawing_revisions` table (migration `009`, append-only, backfilled from each existing drawing's current revision/checker/design_head/status): drawing creation inserts an initial row (revision `A`, `prepared_by` = creator); Checker's checklist APPROVE stamps `checked_by` onto that same latest row; Design Head approval stamps `approved_by`; approving an ECN inserts a brand-new row (new revision number, `prepared_by` = ECN's requested-by, `approved_by` = the Design Head approving it) linked back to the ECN via a nullable `ecn_id` FK — deliberately kept `engineering_change_notices.previous_revision`/`new_revision` as-is (free text) rather than refactoring them to FKs, to avoid touching the already-tested ECN flow; the new table gives full traceability via a join instead. `GET /drawings/:id` now returns a `revisions` array; new "Revision History" section on the Drawing detail page. Verified live end-to-end: initial revision row created correctly on drawing creation, `checked_by`/`approved_by` stamped onto the same row (not new rows) through the checklist/design-head-approve steps, ECN approval appends a genuinely new row linked via `ecn_id` while the original row stays untouched (append-only confirmed), and `drawings.revision` correctly bumps to match. Hit and fixed a real Railway infra issue along the way: `railway up` run from inside `backend/` uploaded a snapshot with no `backend/` subfolder, but the service's configured `rootDirectory` is `backend`, causing the builder to fail unpacking (`no such file or directory`) — running `railway up` from the repo root instead (`Store POC/`) resolved it.
- [x] **Production: Daily Production Entry** ~~(date, planned/actual/balance qty, shift, engineer) — no table~~ — Fixed 2026-08-22. New `daily_production_entries` table (migration `010`), one row per date/shift logged against a Production Schedule activity: Date, Shift, Engineer, Planned Qty, Actual Qty, and Balance Qty (`planned - actual`, computed server-side rather than trusted from the client, so it can't be spoofed or drift). `POST /production/schedules/:id/daily-entries` (Production, Shop Floor Engineer, Admin only); schedule detail response now includes a `daily_entries` array, newest-date first; new "Daily Production Entry" section on the Schedule detail page. Verified live end-to-end: entry against a non-existent schedule rejected (404), balance_qty correctly computed both for under-production (positive balance) and over-production (negative balance, not blocked — SOP doesn't cap actual at planned), `planned_qty <= 0` rejected by validation, listing returns both entries newest-date-first, and a non-Production/Shop-Floor/Admin role (Quality) correctly forbidden (403).
- [x] **Production: Resource Allocation** ~~(Manpower/Machine/Space masters + allocation records) — no tables~~ — Fixed 2026-08-22. New `machines` and `labour` shared masters (migration `011`) — per Recommended Decision #6 these are shared masters alongside Vendor/Item/Project, not Production-local lists, so they get their own top-level routes (`/api/machines`, `/api/labour`) rather than living under `/api/production`. Three allocation-record tables reference them: `machine_allocations` (with a real availability lifecycle — allocating flips the machine to `ALLOCATED` and blocks a second allocation until released; releasing flips it back to `AVAILABLE`), `manpower_allocations` (Employee/Department/Shift/Assigned Job), and `space_allocations` (Fabrication Bay/Painting Bay/Assembly Area — a fixed 3-value enum per the SOP, not a master table, since there's no evidence more bays get added dynamically). New "Resource Allocation" page (masters + all three allocation tables, one page) and sidebar entry. Verified live end-to-end: non-Production/Production-Head/Admin role correctly forbidden from creating a machine, duplicate machine number rejected, allocating flips availability to `ALLOCATED`, a second allocation attempt on an already-allocated machine rejected, releasing flips it back to `AVAILABLE` and a double-release is rejected, re-allocation succeeds after release, manpower allocation against an unknown employee rejected, invalid space enum value rejected, and the combined resource-allocations view correctly joins machine/employee names from the masters.
- [x] **Project/Civil: Civil Execution extension** ~~(BOQ, Rate Chart, Quantity Sheet, Rate Analysis, Estimated Cost) — entirely absent~~ — Fixed 2026-08-22. New `civil_executions` header (migration `012`, one per Project — a genuine extension, not a separate Project entity, matching the SOP's explicit framing) holding three line-item collections: `boq_lines` (optionally linked to a new shared `rate_charts` master, `/api/rate-charts`), `quantity_sheet_lines`, and `rate_analysis_lines` (material/labour/machinery cost inputs with a server-computed rate = costs × (1 + overhead%)). Estimated Cost is never a manually entered field — it's `SUM(boq_lines.amount)`, recomputed server-side on every BOQ line insert, mirroring the same system-computed pattern already used for Excess/Saving (§5 AC4). New "Civil Execution" section on the Project detail page (create record → BOQ/Quantity Sheet/Rate Analysis tables + add dialogs). Verified live end-to-end: BOQ line correctly blocked before the Civil Execution record exists, duplicate record-creation rejected, BOQ line amount = quantity × rate, rate-chart join returns the linked work description, estimated_cost correctly recomputes across 2 and then 3 BOQ lines (47000 → 48000, proving live recomputation not a stale snapshot), rate analysis computed_rate matches the overhead formula exactly, and a non-Project-Manager/Admin role (Quality) correctly forbidden (403).
- [x] **System-wide: file/document upload.** ~~Confirmed zero upload code anywhere~~ — Fixed 2026-08-22. Generic `attachments` table (`entity_type`/`entity_id`, migration `006`) storing files as Postgres `BYTEA` (already this app's durable store; Railway's backend containers are otherwise stateless, so local disk wasn't an option). `POST/GET /api/attachments` + `GET /api/attachments/:id/download` (multer, 15MB cap with a clear 400 on oversize). Reusable `AttachmentsPanel` frontend component — downloads go through axios as a blob rather than a plain `<a href>`, since a bare hyperlink can't carry the required Authorization header. Wired into Drawing detail (Design attachments), Material Receipt detail (Store §1's GRN "Attachments: invoice/photos"), and each Vehicle's insurance record (Transport's "Insurance Document" field) as the first three consumers — reused by more modules going forward rather than rebuilt per-module. Verified live: upload → list → download is byte-exact, correct MIME type preserved, unauthenticated download rejected, invalid `entity_type` rejected, oversized file rejected cleanly.

- [x] **System-wide: PDF/document generation.** ~~Delivery Challan, GA drawings, Payment Vouchers are string/number fields only — nothing produces an actual document~~ — Fixed 2026-08-22. GA Drawings were already resolved as a side effect of the earlier system-wide file-upload work (`AttachmentsPanel` wired to the `drawing` entity — a Design Engineer can upload/download the actual GA drawing PDF). Added real server-generated PDFs for the other two: `backend/src/utils/pdf.js` wraps `pdfkit` (pure-JS, no native/Chromium dependency, so it builds cleanly in the existing Alpine Docker image) into a `streamPdf()` helper. `GET /sales-orders/:id/delivery-challan-pdf` (blocked with a 400 until the order is actually dispatched, since that's when vehicle/driver details exist to print) and `GET /invoices/:id/payments/:paymentId/voucher-pdf` stream real PDFs — downloaded via axios blob (same pattern as file attachments, since a plain `<a href>` can't carry the Bearer token) from new buttons on the Sales Order and Invoice detail pages. Verified live end-to-end: Delivery Challan PDF correctly blocked before dispatch, generates successfully after a full quotation→negotiation→confirm→production→dispatch chain with a valid `%PDF-` header and correct `application/pdf` content-type; Payment Voucher PDF generates for a real recorded payment with a valid `%PDF-` header; both a mismatched invoice and a mismatched payment ID on the voucher route correctly return 404.

All Tier 1 whole-entity gaps are now closed. Moving to Tier 2 next.

### Tier 2 — smaller but real gaps

- [x] Purchase: MR missing `project_id`, `priority`, `required_date`; Budget has no project dimension (department-only currently). — Fixed 2026-08-22 (migration `013`). MR now carries an optional `project_id` FK, `priority` (Low/Medium/High/Urgent, defaults Medium), and `required_date`; the MR list/form surface all three. Budgets can now be allocated either department-wide (`project_id` null) or per-project — enforced via two partial unique indexes (one department-wide row + any number of distinct per-project rows, per department) instead of the old single `UNIQUE(department)`. PO creation and Finance's budget-exceeded approval both resolve the correct budget row: project-specific first if the PO names a project, department-wide otherwise — never both. Scope note: kept `project_id` nullable everywhere rather than making Project mandatory on every MR/PO (as a strict reading of AC1 would require) — Store/Production raise MRs unrelated to a Civil project, and forcing a project link there would break that already-tested flow; the project dimension is additive, not a hard requirement. Verified live end-to-end: MR persists project/priority/required_date correctly and defaults priority to Medium when omitted, MR list joins `project_name`, a department can hold both a department-wide AND a project-specific budget simultaneously without conflict, duplicate department-wide and duplicate project-specific allocations are each rejected, a PO raised against a project debits only that project's budget (department-wide budget untouched), and a PO with no project correctly falls back to the department-wide budget instead.
- [x] Purchase: PO has no delivery-tracking fields (expected/actual delivery date) or "Amended" status; no delay alerts. — Fixed 2026-08-22 (migration `014`). Added `expected_delivery_date` (set at PO creation), `actual_delivery_date` (stamped automatically on every approved GRN against the PO), and a new `AMENDED` status alongside the existing OPEN/PARTIALLY_RECEIVED/CLOSED. `PATCH /purchase-orders/:id/amend` (Purchase/Admin) revises the expected date and requires an `amendment_reason` (captured for free by the audit trigger already on `purchase_orders`) — kept as a real status rather than a silent field edit, per the SOP's explicit enum. Receiving against an OPEN or AMENDED PO both correctly progress it to PARTIALLY_RECEIVED (broadened from the original OPEN-only check). New role-scoped notification-bell alert (`po_delivery_delay`, Purchase/Admin) surfaces POs past their expected delivery date with nothing yet received. Verified live end-to-end: a PO raised with a past expected date immediately triggers the delay alert; amending clears the alert (future date) and sets status/amendment_reason correctly; amending without a reason is rejected by validation; GRN approval against an AMENDED PO stamps `actual_delivery_date` and moves it to PARTIALLY_RECEIVED; a further amendment on a PARTIALLY_RECEIVED PO still succeeds; a non-Purchase/Admin role (Quality) is correctly forbidden (403) from amending.
- [x] Purchase: no Vendor Performance / Delivery Delay reports. — Fixed 2026-08-22. Two new report endpoints (`GET /reports/vendor-performance`, `GET /reports/delivery-delay`) added as new tabs on the Reports page, reusing the tab-driven pattern already established there. Vendor Performance aggregates per supplier (LEFT JOIN so zero-activity vendors still appear with zero counts, not silently dropped): total POs, total value, delivered/on-time/late counts, and average delay days — all computed from `expected_delivery_date`/`actual_delivery_date`, never a manually entered score. Delivery Delay lists every PO either still outstanding past its expected date or delivered late, with `days_delayed` computed server-side in both cases (`CURRENT_DATE - expected` for outstanding, `actual - expected` for late deliveries) and on-time POs correctly excluded. No schema change needed — both reports read the delivery-tracking columns added in the prior PO item. Verified live end-to-end: an overdue-and-still-undelivered PO and a PO delivered late both appear in the delay report with correct `days_delayed`; a PO delivered on time is correctly excluded; the vendor performance report correctly tallies total/delivered/on-time/late counts and average delay for a supplier with real PO history, and a supplier with zero POs still appears with zero counts rather than being filtered out.
- [x] Store: 6 of 12 SOP reports missing (GRN Register, Material Issue Register, Inventory Summary, Rejected Material Report, Scrap Report, Inventory Audit Report). — Fixed 2026-08-22. All 6 added as new tabs on the existing Reports page, no schema changes needed. Scrap Report is a filtered view of `rejected_materials` where `action_taken = 'SCRAP'` rather than a separate scrap entity, matching the SOP's own enum (Return to Supplier / Scrap / Rework / Replacement). Inventory Audit Report reuses the system-wide `audit_log` (built earlier for the audit-trail Tier 0 item) filtered to the `items`/`item_batches` tables and joined back to item code/name, rather than inventing a separate manual stock-take entity — restricted to Store Manager/Admin since it exposes who-changed-what. Goods Receipt Register/Material Issue Register/Inventory Summary reuse the exact `available_stock` formula already established elsewhere in the app for consistency. Verified live end-to-end: GRN Register correctly sums multi-line receipt quantities, Material Issue Register lists an approved MR with `quantity_issued` populated, Inventory Summary's `available_stock`/`total_value` match their formulas exactly, Rejected Material Report and Scrap Report both list a SCRAP-dispositioned rejection while Scrap Report correctly excludes a REWORK-dispositioned one, Inventory Audit Report surfaces a real field-level stock change, and a non-Store-Manager/Admin role (Quality) is correctly forbidden (403) from the audit report.
- [x] Store: dashboard has 6 of 10 SOP KPIs (missing Daily Inward/Outward, Monthly Consumption, Expiring Materials, Inventory Accuracy, Warehouse Utilization). — Fixed 2026-08-22. All 5 added as new StatCards on the Executive Store Overview dashboard, all computed from existing tables (no schema change). Scope note on the two ambiguous ones: the SOP names "Inventory Accuracy" and "Warehouse Utilization" without defining how to compute them, and this schema has no physical stock-take/cycle-count entity or warehouse capacity model — rather than fabricate a number, both are defined as honest, real, clearly-labeled metrics: Inventory Accuracy = % of the catalog where the negative-stock rule the app already enforces (§3) actually holds (a system-integrity check); Warehouse Utilization = % of the catalog with a warehouse location actually assigned (location-tracking coverage). Verified live end-to-end: a same-day IN/OUT stock movement pair correctly moves Daily Inward/Outward and Monthly Consumption by the exact quantities; adding a batch expiring in 10 days correctly increments Expiring Materials; both percentage KPIs return valid 0–100 values.
- [ ] Design: ECN links to a single drawing; SOP describes affected-drawings as a list.
- [ ] Accounts: no Document Archive (searchable by invoice/party/date); no Voucher Number; Payment has one generic `reference_number` instead of distinct Bank/UTR-or-Cheque fields.
- [ ] Transport: `insurance_records.document_name` is a filename string, not real file storage (depends on the system-wide upload task above).

### Confirmed intentionally out of scope (not gaps)

GST Reconciliation (Accounts, Phase 2 per Recommended Decision #8), mobile/offline DPR sync (Recommended Decision #10), SMS/WhatsApp notification channels (optional per SOP) — all explicitly deferred already, not oversights.

## Next Up (requested, not started)

1. **Push to GitHub** (this session) — repo: `https://github.com/gokulrrathod/company-store-erp.git`.
2. **Railway deployment** — feasibility discussed and confirmed workable; user is doing this next.

## Notes for next session

- Seeded test users: `<role>@test.com` / `Password@123` for all (see `db/seed.sql`) — e.g. `admin@test.com`, `transport@test.com`, `accounts@test.com`, `projectmanager@test.com`, `siteengineer@test.com`, etc.
- Standard rebuild cycle after any schema change: `docker compose down -v && docker compose up -d --build`, then `curl http://localhost:4000/api/health` and check `http://localhost:8081`.
- Full regression command: `cd e2e && npx playwright test --workers=1 --reporter=list` (workers>1 causes flaky login timeouts against the single Docker backend — not a real bug, confirmed by isolated re-runs).
- Recurring Postgres gotcha to watch for in any new SQL: `CASE WHEN $1 = 'STRING'` throws error 42P08 unless cast as `$1::varchar`.
